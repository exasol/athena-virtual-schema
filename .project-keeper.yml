sources:
  - type: maven
    path: pom.xml
    modules:
      - jar_artifact
linkReplacements:
build:
  runnerOs: ubuntu-latest # Exasol 7.1.x does not yet support the latest Ubuntu version
  exasolDbVersions:
    - "2026.1.0"
    - "2025.1.10"
